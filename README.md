# Workverse
Welcome to The Virtual Workplace


# To run the client:

`cd metaverse-worktez`<br />
`npm install`<br />
`ng serve`<br />

# To run the Server:

`cd server`<br />
`npm install`<br />
`npm run start`<br />
